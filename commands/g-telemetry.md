---
description: Compute the 8 reliability metrics and derive a project health profile (stable / cautious / defensive / recovery). Writes .claude/telemetry-profile for adaptive orchestration in /g-execute and /g-review. Read-only on history.
---

Use Glob to find `skills/g-telemetry/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly.
