---
description: Auto-detect stack from project brief and dependencies, or apply a named profile. Installs architect agent and architecture rules into the project.
argument-hint: [stack]
---

Use Glob to find `skills/g-team-specialize/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
