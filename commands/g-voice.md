---
description: Switch the G-Forge voice profile — `dev` (terse, jargon-dense — default), `mid` (explained-but-concise), or `eli5` (plain language, conversational). No argument shows the current profile with side-by-side samples of all three.
---

Use Glob to find `skills/g-voice/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
