---
description: Scan for algorithmic complexity problems, N+1 queries, re-render waste, resource leaks, and caching opportunities. Targeted scope produces an inline report; whole-codebase scope produces a prioritised roadmap milestone.
argument-hint: [path/to/scope | all]
---

Use Glob to find `skills/g-optimize/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
