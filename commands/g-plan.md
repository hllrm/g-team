---
description: Decompose the current request into atomic tasks and produce a parallel wave schedule. Use at the start of any multi-step implementation.
---

Use Glob to find `skills/g-team-plan/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly.
