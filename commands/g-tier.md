---
description: Switch G-Forge integration tier — `full` (all hooks + auto-triggers), `balanced` (state hooks only, no auto-triggers), or `light` (workflow-checkpoint only, commit gate off). No argument shows current tier.
---

Use Glob to find `skills/g-tier/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
