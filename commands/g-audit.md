---
description: Scan for SOLID violations, code smells, architectural drift, dead code, and test coverage gaps. Targeted scope produces an inline report; whole-codebase scope produces a prioritised roadmap milestone.
argument-hint: [path/to/scope | all]
---

Use Glob to find `skills/g-audit/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
