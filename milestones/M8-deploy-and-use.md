# M8 — Deploy & Use

## Goal
Self-host g-team on the g-team repo, add a `claude-plugin` stack profile, and add vibecoding skills for designing and validating Claude Code skills/agents.

## Scope
- [ ] Install g-team into this repo (CLAUDE.md, hooks, settings.json, milestone files)
- [ ] Create `milestones/M6`, `milestones/M7` files (retroactive)
- [ ] `claude-plugin` stack profile — architect agent + architecture rules
- [ ] `/g-team skill-design` skill — guided workflow for designing new skills/agents
- [ ] `/g-team skill-validate` skill — validates SKILL.md and agent files against quality criteria
- [ ] Register `skill-design` and `skill-validate` in `commands/g-team.md` router

## Done condition
`/g-team doctor` reports 9/9 on this repo. `claude-plugin` profile installs cleanly via `/g-team specialize`. `skill-design` and `skill-validate` skills are reachable via `/g-team skill-design` and `/g-team skill-validate`.

## Status
🚧 In progress
